{
	"name": "@mysten/gcp-kms-signer",
	"author": "Mysten Labs <build@mystenlabs.com>",
	"description": "Sui signer using Google Cloud KMS",
	"homepage": "https://sdk.mystenlabs.com",
	"version": "0.2.20",
	"license": "Apache-2.0",
	"type": "module",
	"sideEffects": false,
	"exports": {
		".": {
			"types": "./dist/index.d.mts",
			"import": "./dist/index.mjs",
			"default": "./dist/index.mjs"
		}
	},
	"files": [
		"CHANGELOG.md",
		"LICENSE",
		"README.md",
		"dist",
		"src"
	],
	"scripts": {
		"clean": "rm -rf tsconfig.tsbuildinfo ./dist",
		"build": "rm -rf dist && tsc --noEmit && tsdown",
		"prettier:check": "prettier -c --ignore-unknown .",
		"prettier:fix": "prettier -w --ignore-unknown .",
		"oxlint:check": "oxlint  .",
		"oxlint:fix": "oxlint --fix",
		"lint": "pnpm run oxlint:check && pnpm run prettier:check",
		"lint:fix": "pnpm run oxlint:fix && pnpm run prettier:fix",
		"test": "vitest run"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/MystenLabs/ts-sdks.git"
	},
	"bugs": {
		"url": "https://github.com/mystenlabs/ts-sdks/issues"
	},
	"publishConfig": {
		"access": "public"
	},
	"engines": {
		"node": ">=22"
	},
	"dependencies": {
		"@google-cloud/kms": "^6.0.0",
		"@noble/curves": "^2.3.0",
		"asn1-ts": "^11.1.0"
	},
	"peerDependencies": {
		"@mysten/sui": "workspace:^"
	},
	"devDependencies": {
		"@types/node": "^26.2.0",
		"dotenv": "^17.4.2",
		"typescript": "^7.0.2",
		"vitest": "^4.1.10"
	}
}
