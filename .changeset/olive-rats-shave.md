---
'@mysten/docs': patch
---

Move the `next` dependency from `^16.3.1` to `^16.3.4`, which carries the fix for a critical
advisory against `next` <16.3.3 and for a high advisory against the `sharp` it pulls in. No API or
behavior change.
