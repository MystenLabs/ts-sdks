---
'@mysten/sui': minor
---

Add `isValidStructTag` and `isValidMoveIdentifier` validation utilities. `isValidNamedType` now also validates the full struct tag structure (completing the TODO that previously returned `true` unconditionally).
