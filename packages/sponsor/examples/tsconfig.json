{
	"extends": "../tsconfig.json",
	"include": ["**/*.ts", "../src/**/*.ts"],
	"compilerOptions": {
		"noEmit": true,
		"rootDir": "..",
		"types": ["node"],
		"module": "preserve",
		"moduleResolution": "bundler"
	}
}
