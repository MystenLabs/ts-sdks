---
'@mysten/sui': minor
---

Add `isValidStructTag` validation utility. `isValidNamedType` now also validates the full struct tag structure (completing the TODO that previously returned `true` unconditionally).
