---
'@mysten/sui': minor
---

Export `isCoinReservationDigest` and `parseCoinReservationBalance` from `@mysten/sui/utils` for use by downstream packages that need to detect and parse the synthetic coin reservation refs used in gas payment arrays.
