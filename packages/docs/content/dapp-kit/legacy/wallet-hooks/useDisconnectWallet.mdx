---
title: useDisconnectWallet
description: Legacy React mutation hook to disconnect from the currently connected wallet.
keywords:
  - useDisconnectWallet
  - React hook
  - disconnect
  - wallet
  - legacy
  - dApp Kit
  - Sui
questions:
  - What does the useDisconnectWallet hook do?
  - How do I use useDisconnectWallet?
  - How does Arguments work?
answer: Legacy React mutation hook to disconnect from the currently connected wallet.
goal:
  description: >-
    Reader can use the legacy React mutation hook to disconnect from the currently connected wallet
  requires:
    - has_frontmatter:
        - title
        - description
        - keywords
      label: Has required frontmatter fields
    - min_words: 30
      label: Needs content depth
    - has_questions: true
      label: Needs questions for AI search visibility
    - has_answer: true
      label: Needs answer summary for AI citation
---

import { UseDisconnectWalletExample } from '../../../../examples/wallet-hooks-client';

The `useDisconnectWallet` hook is a mutation hook for disconnecting from an active wallet
connection, if currently connected.

```ts
import { ConnectButton, useDisconnectWallet } from '@mysten/dapp-kit';

function MyComponent() {
	const { mutate: disconnect } = useDisconnectWallet();
	return (
		<div>
			<ConnectButton />

			<button onClick={() => disconnect()}>Disconnect</button>
		</div>
	);
}
```

## Example

<UseDisconnectWalletExample />

## Arguments

There are no arguments for `useDisconnectWallet`.
