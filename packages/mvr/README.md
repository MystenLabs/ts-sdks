# mvr SDK

The mvr SDK is a TypeScript SDK for the Move Registry (mvr). There are two use cases:
1. Generate a static file for MVR resolution (to avoid runtime querying)
2. Query MVR data / utilities

## Usage

### Generate a static file for MVR resolution

```bash
npx @mysten/mvr --directory .
```
